var disableSpouseForMinor = function (patient){
    var returnValues = {
        show:[],
        hide:[]
    };
    if (patient["age"].years < 10){
        //disable Spouse relation
        //else enable
    }
}